const { validateCreateAnalysis } = require('./validateCreateAnalysis')
const { validateUpdateAnalysis } = require('./validateUpdateAnalysis')
module.exports = {
  validateCreateAnalysis,
  validateUpdateAnalysis
}
